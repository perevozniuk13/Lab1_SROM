A = "4D3C91C579C2C6216567A5241614B561ADDF76C4BB659E6FE7F65FF76A918C843F0458B3EF457BCD9022D78798A29462EC99C74E6674690267D3E9844251B39D"
B = "DAF1ABDA4AD4D9FE3E36A529210C2AE99B905922FC0519798A26E351FE23AF375AD6BA288EE030B70DF0CE1CDF1E8B75BA56494DC6ED36B181814CD5783E6C81"
l = len(A)
x = l % 8
k = l + 8 - x
import textwrap
if x == 0 :
	listA = textwrap.wrap(A, 8)
	for (index, elem) in enumerate(listA):
                  listA[index] = int(elem, 16)
                  print(listA.reverse)
else:
	s = s.rjust(k, '0')
	listA = textwrap.wrap(A, 8)
	for (index, elem) in enumerate(listA):
                  listA[index] = int(elem, 16)
                  print(listA.reverse)

l = len(B)
if x == 0 :
	listB = textwrap.wrap(B, 8)
	for (index, elem) in enumerate(listB):
                  listB[index] = int(elem, 16)
                  print(listB.reverse)
else:
	s = s.rjust(k, '0')
	listA = textwrap.wrap(B, 8)
	for (index, elem) in enumerate(listA):
                  listB[index] = int(elem, 16)
                  print(listB.reverse)


#ADDITION
listC = [ ]
carry = 0
for index in range(len(listA)):
    temp = listA[index] + listB[index] + carry
    m = temp & ((2 ** 32) - 1)
    listC.insert(index, m)
    carry = temp >> 32
>>
print(listC)

#SUBTRACTION
listC = [ ]
borrow = 0
for index in range(len(listA)):
    temp = listA[index] - listB[index] - borrow
    if temp >= 0 :
            listC.insert(index, temp)
            borrow =0
    else:
            listC.insert(index, (2 ** 32) + temp)
            borrow = 1
>>
print(listC)
